{
  "note": "Generated by record-node-schema.ts from apps/server/src/db.ts initializeDatabase (seedDefaults: false). Do not edit by hand.",
  "node": "v22.16.0",
  "betterSqlite3": "11.10.0",
  "sqlite": "3.49.2",
  "fresh": [
    {
      "type": "table",
      "name": "categories",
      "tbl_name": "categories",
      "sql": "CREATE TABLE categories (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      name TEXT NOT NULL,\n      slug TEXT NOT NULL UNIQUE,\n      description TEXT NOT NULL DEFAULT '',\n      color TEXT NOT NULL DEFAULT '#6366f1'\n    )"
    },
    {
      "type": "table",
      "name": "authors",
      "tbl_name": "authors",
      "sql": "CREATE TABLE authors (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      name TEXT NOT NULL,\n      email TEXT NOT NULL UNIQUE,\n      password_hash TEXT NOT NULL,\n      avatar TEXT,\n      bio TEXT,\n      role TEXT NOT NULL DEFAULT 'editor' CHECK(role IN ('admin', 'editor'))\n    )"
    },
    {
      "type": "table",
      "name": "articles",
      "tbl_name": "articles",
      "sql": "CREATE TABLE articles (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      title TEXT NOT NULL,\n      slug TEXT NOT NULL UNIQUE,\n      excerpt TEXT NOT NULL DEFAULT '',\n      content TEXT NOT NULL DEFAULT '',\n      featured_image TEXT,\n      category_id INTEGER NOT NULL,\n      author_id INTEGER NOT NULL,\n      status TEXT NOT NULL DEFAULT 'draft' CHECK(status IN ('draft', 'published', 'scheduled')),\n      published_at TEXT,\n      meta_title TEXT,\n      meta_description TEXT,\n      source TEXT,\n      source_url TEXT,\n      view_count INTEGER NOT NULL DEFAULT 0,\n      created_at TEXT NOT NULL DEFAULT (datetime('now')),\n      updated_at TEXT NOT NULL DEFAULT (datetime('now')),\n      FOREIGN KEY (category_id) REFERENCES categories(id),\n      FOREIGN KEY (author_id) REFERENCES authors(id)\n    )"
    },
    {
      "type": "table",
      "name": "media",
      "tbl_name": "media",
      "sql": "CREATE TABLE media (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      filename TEXT NOT NULL,\n      url TEXT NOT NULL,\n      mime_type TEXT NOT NULL,\n      size INTEGER NOT NULL,\n      uploaded_at TEXT NOT NULL DEFAULT (datetime('now'))\n    )"
    },
    {
      "type": "table",
      "name": "settings",
      "tbl_name": "settings",
      "sql": "CREATE TABLE settings (\n      key TEXT PRIMARY KEY,\n      value TEXT NOT NULL\n    )"
    },
    {
      "type": "table",
      "name": "subscribers",
      "tbl_name": "subscribers",
      "sql": "CREATE TABLE subscribers (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      email TEXT NOT NULL UNIQUE,\n      status TEXT NOT NULL DEFAULT 'pending',\n      source_placement TEXT,\n      confirmation_sent_at TEXT,\n      confirmed_at TEXT,\n      unsubscribed_at TEXT,\n      created_at TEXT NOT NULL DEFAULT (datetime('now')),\n      updated_at TEXT NOT NULL DEFAULT (datetime('now'))\n    )"
    },
    {
      "type": "table",
      "name": "newsletter_deliveries",
      "tbl_name": "newsletter_deliveries",
      "sql": "CREATE TABLE newsletter_deliveries (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      subscriber_id INTEGER NOT NULL,\n      edition_key TEXT NOT NULL,\n      status TEXT NOT NULL DEFAULT 'sending',\n      provider_message_id TEXT,\n      error TEXT,\n      created_at TEXT NOT NULL DEFAULT (datetime('now')),\n      sent_at TEXT,\n      UNIQUE(subscriber_id, edition_key),\n      FOREIGN KEY (subscriber_id) REFERENCES subscribers(id) ON DELETE CASCADE\n    )"
    },
    {
      "type": "table",
      "name": "newsletter_editions",
      "tbl_name": "newsletter_editions",
      "sql": "CREATE TABLE newsletter_editions (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      edition_key TEXT NOT NULL UNIQUE,\n      subject TEXT NOT NULL,\n      articles TEXT NOT NULL,\n      created_at TEXT NOT NULL DEFAULT (datetime('now'))\n    )"
    },
    {
      "type": "index",
      "name": "idx_articles_source_url",
      "tbl_name": "articles",
      "sql": "CREATE INDEX idx_articles_source_url ON articles(source_url)"
    },
    {
      "type": "index",
      "name": "idx_subscribers_status",
      "tbl_name": "subscribers",
      "sql": "CREATE INDEX idx_subscribers_status ON subscribers(status)"
    },
    {
      "type": "index",
      "name": "idx_newsletter_deliveries_edition",
      "tbl_name": "newsletter_deliveries",
      "sql": "CREATE INDEX idx_newsletter_deliveries_edition ON newsletter_deliveries(edition_key, status)"
    }
  ],
  "legacy": [
    {
      "type": "table",
      "name": "articles",
      "tbl_name": "articles",
      "sql": "CREATE TABLE articles (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      title TEXT NOT NULL,\n      slug TEXT NOT NULL UNIQUE,\n      excerpt TEXT NOT NULL DEFAULT '',\n      content TEXT NOT NULL DEFAULT '',\n      featured_image TEXT,\n      category_id INTEGER NOT NULL,\n      author_id INTEGER NOT NULL,\n      status TEXT NOT NULL DEFAULT 'draft' CHECK(status IN ('draft', 'published', 'scheduled')),\n      published_at TEXT,\n      meta_title TEXT,\n      meta_description TEXT,\n      view_count INTEGER NOT NULL DEFAULT 0,\n      created_at TEXT NOT NULL DEFAULT (datetime('now')),\n      updated_at TEXT NOT NULL DEFAULT (datetime('now')), source TEXT, source_url TEXT,\n      FOREIGN KEY (category_id) REFERENCES categories(id),\n      FOREIGN KEY (author_id) REFERENCES authors(id)\n    )"
    },
    {
      "type": "table",
      "name": "subscribers",
      "tbl_name": "subscribers",
      "sql": "CREATE TABLE subscribers (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      email TEXT NOT NULL UNIQUE\n    , status TEXT NOT NULL DEFAULT 'pending', source_placement TEXT, confirmation_sent_at TEXT, confirmed_at TEXT, unsubscribed_at TEXT, created_at TEXT, updated_at TEXT)"
    },
    {
      "type": "table",
      "name": "categories",
      "tbl_name": "categories",
      "sql": "CREATE TABLE categories (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      name TEXT NOT NULL,\n      slug TEXT NOT NULL UNIQUE,\n      description TEXT NOT NULL DEFAULT '',\n      color TEXT NOT NULL DEFAULT '#6366f1'\n    )"
    },
    {
      "type": "table",
      "name": "authors",
      "tbl_name": "authors",
      "sql": "CREATE TABLE authors (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      name TEXT NOT NULL,\n      email TEXT NOT NULL UNIQUE,\n      password_hash TEXT NOT NULL,\n      avatar TEXT,\n      bio TEXT,\n      role TEXT NOT NULL DEFAULT 'editor' CHECK(role IN ('admin', 'editor'))\n    )"
    },
    {
      "type": "table",
      "name": "media",
      "tbl_name": "media",
      "sql": "CREATE TABLE media (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      filename TEXT NOT NULL,\n      url TEXT NOT NULL,\n      mime_type TEXT NOT NULL,\n      size INTEGER NOT NULL,\n      uploaded_at TEXT NOT NULL DEFAULT (datetime('now'))\n    )"
    },
    {
      "type": "table",
      "name": "settings",
      "tbl_name": "settings",
      "sql": "CREATE TABLE settings (\n      key TEXT PRIMARY KEY,\n      value TEXT NOT NULL\n    )"
    },
    {
      "type": "table",
      "name": "newsletter_deliveries",
      "tbl_name": "newsletter_deliveries",
      "sql": "CREATE TABLE newsletter_deliveries (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      subscriber_id INTEGER NOT NULL,\n      edition_key TEXT NOT NULL,\n      status TEXT NOT NULL DEFAULT 'sending',\n      provider_message_id TEXT,\n      error TEXT,\n      created_at TEXT NOT NULL DEFAULT (datetime('now')),\n      sent_at TEXT,\n      UNIQUE(subscriber_id, edition_key),\n      FOREIGN KEY (subscriber_id) REFERENCES subscribers(id) ON DELETE CASCADE\n    )"
    },
    {
      "type": "table",
      "name": "newsletter_editions",
      "tbl_name": "newsletter_editions",
      "sql": "CREATE TABLE newsletter_editions (\n      id INTEGER PRIMARY KEY AUTOINCREMENT,\n      edition_key TEXT NOT NULL UNIQUE,\n      subject TEXT NOT NULL,\n      articles TEXT NOT NULL,\n      created_at TEXT NOT NULL DEFAULT (datetime('now'))\n    )"
    },
    {
      "type": "index",
      "name": "idx_articles_source_url",
      "tbl_name": "articles",
      "sql": "CREATE INDEX idx_articles_source_url ON articles(source_url)"
    },
    {
      "type": "index",
      "name": "idx_subscribers_status",
      "tbl_name": "subscribers",
      "sql": "CREATE INDEX idx_subscribers_status ON subscribers(status)"
    },
    {
      "type": "index",
      "name": "idx_newsletter_deliveries_edition",
      "tbl_name": "newsletter_deliveries",
      "sql": "CREATE INDEX idx_newsletter_deliveries_edition ON newsletter_deliveries(edition_key, status)"
    }
  ]
}
